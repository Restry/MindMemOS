[project]
name = "mindmemos-skill"
version = "0.1.0"
description = "Skill components for MindMemOS"
readme = "README.md"
authors = []
requires-python = ">=3.11,<3.14"
dependencies = ["pydantic>=2.0"]

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = "."
